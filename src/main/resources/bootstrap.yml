# spring:
#   cloud:
#     config:
#       name: batch-service
#       discovery:
#         service-id: config-service
#         enabled: true
spring:
  profiles:
    active: ${ACTIVE_PROFILES:default}
  cloud:
    config:
      uri: ${CONFIG_URL}