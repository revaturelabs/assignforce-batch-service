# spring:
#   cloud:
#     config:
#       name: batch-service
#       discovery:
#         service-id: config-service
#         enabled: true
spring:
  cloud:
    config:
      uri: http://${CONFIG_USERNAME}:${CONFIG_PASSWORD}@${CONFIG_URL}
  profiles:
    active: ${ACTIVE_PROFILES:default}